const Discord = require("discord.js");
const fetch = require('node-fetch');


module.exports.run = async(client, message, args) => {
 //  const link = args[0];
 //  let { args[1] } = await fetch("https://www.strawpoll.me/api/v2/polls/" + link).then(r => r.json());
  // let fileString = JSON.stringify(args[0]);
 //  console.log(fileString);
  // message.channel.send(fileString);
}

module.exports.config = {
    name: "webrequest",
    aliases: ["wr"]
}